// Find the <header> element using document.querySelector
const header = document.querySelector('header');

// Update the text color to red (#FF0000) if the element exists
header.style.color = '#FF0000';